# Owner actions before the live demo

The [public app](https://closeout-ashen.vercel.app) and [GitHub repository](https://github.com/mohit-1710/closeout) are available. Public deployment reads and the first cloud CI run passed. Mohit is completing the ETHGlobal form in Brave; the build agent has stopped controlling that browser.

1. **Allow the deployed Privy origin:** Under Closeout → Basics → Domains, add **https://closeout-ashen.vercel.app**, keeping both localhost origins. At September 12, 15:58 UTC, public app configuration still listed only `http://localhost:3000` and `http://127.0.0.1:3000`. The public App ID is configured in Vercel; deployed wallet login remains unverified until the domain is added and checked. The four local chooser checks passed.
2. **Revoke the exposed Privy secret:** Closeout uses only the public App ID; the secret pasted in conversation is unused and should be revoked in Privy. No replacement secret is needed.
3. **Own-account verification:** Connect the owner signer of an existing funded/approved Polymarket account and load its account-wallet address. First verify read-only positions, balances, allowances and the review quote. A real order requires your separate, explicit per-order authorization in the app; none has been sent by the build agent.
4. **Hackathon participation and repository:** Verify your private ETHOnline dashboard shows accepted/staked eligibility and select `mohit-1710/closeout` as its repository. Mohit reports installing the ETHGlobal GitHub app. The CLI credential cannot list app installations, so the connection is not independently verified; the repository appearing in ETHGlobal's selector is the useful confirmation.
5. **Demo and final submission:** Record a 2–4 minute human-voiced walkthrough, at least 720p, using the [demo script](docs/demo-script.md) and [video checklist](docs/video-checklist.md). Show example mode honestly if a real trade is not verified. Add the recording URL and use the [copyable fields](docs/submission.html) to finish the form. Submit by **September 13, 2026 16:00 UTC /21:30 IST**; target 20:30 IST. Use the event dashboard's current requirements if they differ.

No completed event submission or demo recording is verified. No outreach, token approval, wallet funding or trade was performed by this build. The public repository preserves its actual commit dates; its large initial snapshot remains visible to judges.
